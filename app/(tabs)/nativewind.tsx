import { Text, View } from "react-native";

export default function NativeWindTab() {
  // This is just a placeholder screen
  // The tab press will navigate directly to nativewindui-example.tsx
  return (
    <View style={{ flex: 1, justifyContent: "center", alignItems: "center" }}>
      <Text>Redirecting to NativeWindUI Example...</Text>
    </View>
  );
}
